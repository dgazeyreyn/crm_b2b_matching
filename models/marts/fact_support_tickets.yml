models:
  - name: fact_support_tickets
    columns:
      - name: primary_key
        data_tests:
          - unique
          - not_null
      - name: customer_segment
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - ENT
                  - MM
                  - SMB