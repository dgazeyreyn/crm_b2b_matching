version: 2

models:
  - name: dim_tenant
    description: "Dimension table representing customer tenants including segmentation, industry, and firmographic attributes."
    columns:
      - name: tenant_id
        description: "Primary key representing a unique tenant/customer."
        tests:
          - not_null
          - unique
