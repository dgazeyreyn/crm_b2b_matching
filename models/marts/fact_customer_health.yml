models:
  - name: fact_customer_health
    columns:
      - name: tenant_id
        data_tests:
          - unique
          - not_null
      - name: customer_segment
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - ENT
                  - MM
                  - SMB