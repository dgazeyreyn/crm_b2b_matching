models:
  - name: int_support_ticket
    columns:
      - name: ticket_id
        data_tests:
          - unique
          - not_null
      - name: customer_segment
        data_tests:
         - accepted_values:
             arguments:
               values:
                 - ENT
                 - MM
                 - SMB
